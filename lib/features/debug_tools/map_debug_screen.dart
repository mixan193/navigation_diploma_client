import 'package:flutter/material.dart';

class MapDebugScreen extends StatelessWidget {
  const MapDebugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Map Debug'));
  }
}
import 'package:flutter/material.dart';

class PositionDebugScreen extends StatelessWidget {
  const PositionDebugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Position Debug'));
  }
}